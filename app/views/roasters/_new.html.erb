
<%= simple_form_for @roaster, html: { multipart: true } do |f| %>
  <%= f.input :name %>
  <%= f.input :location %>
  <%= f.input :year_established %>
  <%= f.input :image_url, as: :file %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
