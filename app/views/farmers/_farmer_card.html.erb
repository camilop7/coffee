<div class="card-content">
  <div class="left">
    <% if @farmer.present? %>
      <h3><%= @farmer.name %></h3>
      <p><%= @farmer.location %></p>
    <% else %>
      <p>Farmer not found.</p>
    <% end %>
    <button class="card-button">Button</button>
  </div>
</div>
